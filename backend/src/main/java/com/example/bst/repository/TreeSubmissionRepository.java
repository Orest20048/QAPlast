package com.example.bst.repository;

import com.example.bst.model.TreeSubmission;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TreeSubmissionRepository extends JpaRepository<TreeSubmission, Long> {
}
